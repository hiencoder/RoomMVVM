package com.example.hiennv.basicsampleroomdatabinding.callback;

import com.example.hiennv.basicsampleroomdatabinding.model.Comment;

public interface CommentClickListener {
    void onClick(Comment comment);
}
